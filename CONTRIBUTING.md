# Contributing

- Keep implementation layered: model functions, hooks, primitives, styled
  components, and examples belong in separate modules.
- Public behavior needs tests, Storybook coverage, and docs.
- Use CSS custom properties for styling and expose every public token.
